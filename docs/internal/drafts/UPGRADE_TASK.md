# UPGRADE TASK — whatifd gap bridge, cycle 1

> Paste-ready mission file for Claude Code at the `whatifd` repo root.
> Skill required: `whatifd-gap-bridge` (install to `.claude/skills/whatifd-gap-bridge/`).
> Generated 2026-06-13 against `main` @ 47869c1 (v0.3.0). If `main` has moved, the
> skill's Phase 0.5 handles it — do not edit this file to "update" it.

---

## Mission

Review the whatifd project against the seeded gap hypotheses and a fresh scan,
produce an evidence-backed bridge plan as `docs/internal/GAPLEDGER.md`, get it
human-approved (PR #0), then execute the plan in a self-managed loop — one unit,
one branch, one PR per iteration — until every unit is terminal and every public
page is provably consistent with the code. Consistency is proven only by
`scripts/consistency_check.py` exiting 0 on the real tree **and** 0 on
`--self-test` in the same session.

## Operating rules (non-negotiable; full set = SKILL.md cardinal rules)

Never push to `main`; never merge your own PRs; never bump versions, tag, or
release; never edit released CHANGELOG sections; CODE-lane gaps are *promoted*
into `whatifd-design/references/phases.md` or `whatif-features`, not implemented
here; anything Datadog-positioning-adjacent is a written brief +
`AWAITING_HUMAN`, always.

## Phase plan & human gates

1. **Phase 0–2 (interactive session, today):** preflight + negative control →
   evidence sweep (verify the appendix below against the live tree; run the full
   checker on both repos; manual S4 checks incl. executing the site's 60-second
   demo in a clean venv) → write the ledger → open **PR #0** (ledger only).
   ▸ **HUMAN GATE A:** PR #0 review. The Datadog brief (H-17) and the
   private-docs-repo brief (H-20) must be at the top of the PR description.
2. **Phase 3 (unattended loop):** launch only after Gate A. Driver of choice:

   ```
   /goal Run the whatifd-gap-bridge Phase-3 loop. Complete when the final
   printed LEDGER STATUS block shows open: 0 and pr_open: 0 with all remaining
   units terminal, AND the transcript shows consistency_check.py exiting 0 on
   --repo . and 0 on --self-test in this session, AND the Phase-4 closeout PR
   number is printed. Constraints: never push to main, never merge any PR,
   never modify released CHANGELOG sections, one unit per branch. Stop after
   at most 40 turns even if incomplete, printing a final LEDGER STATUS.
   ```

   Overnight alternative: `gap-loop.sh` from the skill's
   `references/loop-protocol.md` (Driver B), scheduled via `/loop` or cron.
   ▸ **HUMAN GATE B (recurring):** merging the per-unit PRs. The loop treats
   un-merged PRs as `PR_OPEN`, not failures.
3. **Phase 4 (closeout):** final dual-verdict consistency run, DEFERRED
   migration to `whatif-features`, closeout report, final PR.
   ▸ **HUMAN GATE C:** closeout PR + the `AWAITING_HUMAN` inbox (expected
   inhabitants: Marketplace publication H-14, naming ratification H-19,
   docs-repo visibility H-20, Datadog positioning H-17, distribution
   publishing H-16).

## Definition of done

- [ ] `docs/internal/GAPLEDGER.md` exists with a valid SOURCE-LINEAGE header.
- [ ] Every unit terminal: DONE / REJECTED / DEFERRED / BLOCKED / AWAITING_HUMAN.
- [ ] `consistency_check.py --repo .` → exit 0, and with `--site-dir <whatifd-docs>` → exit 0.
- [ ] `consistency_check.py --self-test` → exit 0 in the same session as the runs above.
- [ ] Every CONFIRMED CODE gap has a merged promotion PR in `phases.md` or a
      triggered `whatif-features` entry — no orphaned confirmed gaps.
- [ ] Closeout report written; AWAITING_HUMAN inbox listed explicitly.

## Seed evidence appendix (verified 2026-06-13; re-verify before acting)

Mechanical scan of `main` @ 47869c1 — 24 findings (9 DRIFT, 14 WARN, 1 INFO).
The DRIFT set, with two findings beyond the original hypothesis list (*):

| where | finding | maps to |
|---|---|---|
| README.md:26 | install line omits shipped `whatifd-datadog` | H-02 |
| README.md:28 | shipped `whatifd-datadog` called "in-development" | H-02 |
| RELEASING.md:36,89,100,101 | "three adapters / four packages" vs 4 adapters / 5 packages | H-03 |
| SECURITY.md:10 * | supported-versions table marks 0.1.x "(planned)" at v0.3.0 | new unit |
| .claude/skills/whatifd-design/references/statistical-defaults.md:143 | "sequential testing (v0.3)" — v0.3 shipped without it; reference now stale | H-04-adjacent |
| docs/concepts.md:124 * | dead relative link to `path-z.md` (page exists on the site, file absent in repo) | new unit |
| AGENT_TELEMENTRY.md | filename misspelling | H-18 |
| docs/walkthroughs/*, design-skill refs | 13 dead `manifest.json` relative links — triage: committed artifacts vs generated-on-demand | new unit(s) |
| whatif.codes index (fetched 2026-06-13) | Status table: v0.2 "shipped (latest)", v0.3 "planned" w/ cluster bootstrap + LangSmith + Marketplace — contradicts shipped v0.3.0 | H-01 |
| github repo description | opens "whatif is an open experiment runner…" | H-19 |

Unshipped-but-promised (CODE-lane promotions, not doc deletions): cluster-paired
bootstrap (H-04), LangSmith adapter (H-11), live-tool replay — all named on the
site's v0.3 row; none in the tree. Strategic hypotheses H-05..H-17 require the
full Phase-1 verification pass; treat none as confirmed yet.

## Kickoff (copy-paste)

```bash
cd ~/src/whatifd && git switch main && git pull
gh repo clone victoralfred/whatifd-docs ../whatifd-docs || echo "docs repo blocked — flag H-20, proceed against the rendered site"
mkdir -p .claude/skills && cp -r <download-path>/whatifd-gap-bridge .claude/skills/
python .claude/skills/whatifd-gap-bridge/scripts/consistency_check.py --self-test --repo .
# then, in Claude Code:
#   "Using the whatifd-gap-bridge skill, run Phase 0 through Phase 2 and open PR #0."
```
