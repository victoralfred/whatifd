---
session_id: 2026-05-05-pr-23-review
started_at: 2026-05-05T12:29:18+02:00
---

## Session start

**User request:** Review PR #23 (Phase 2.5 — guard protocol + first two guards) and write findings to `../findings/` with PR number and timestamp in the filename. Read project skill, doctrine, and references first.

**Skill files read:**
- .claude/skills/whatifd-design/SKILL.md
- .claude/skills/whatifd-design/references/doctrine.md
- .claude/skills/whatifd-design/references/type-model.md
- .claude/skills/whatifd-design/references/enforcement.md
- .claude/skills/whatifd-design/references/contracts.md
- .claude/skills/whatifd-design/references/practices.md
- .claude/skills/whatifd-design/references/statistical-defaults.md

**Cardinal rules cited:**
- Rule #1 (failure-as-data): the malformed-DecimalString path must raise, not silently abstain — review confirms PR commit `8b74855` corrected this.
- Rule #2 (trust floor cannot be bypassed): guards operate above the floor; no policy path produces Ship without going through evaluate_floor.
- Rule #6 (public schema hand-written; details must be JsonPrimitive): __init__.py discipline note is preventive — confirmed in scope.
- Rule #10 (statistical claims must match the design): `practical_delta_below_threshold` is the magnitude layer; the primary endpoint (rate-based) is the deferred `failure_improvement_below_threshold` guard.

**Clarifying questions asked:**
- none — task was unambiguous (review PR #23, write findings)

**Phase plan position (per references/phases.md):**
- Phase: 2.5 (decision pipeline — guard protocol + first two guards)
- Sub-item: review of in-flight PR; subsequent guards (5 of 7) deferred per cascade entry "Phase 2.5 deferred guards — dependency map"
- Prerequisites status: Phase 2.1–2.4 green (floor evaluator, finding-code registry, fix-suggestion registry).

## Session end

**Artifacts produced:**
- ../findings/PR-23-2026-05-05T12-29-18.md: review findings document (this session's deliverable)
- ./docs/sessions/2026-05-05-pr-23-review.md: this session log

**Cascade catalog items:**
- none (review only — no new entries opened, none resolved). Two existing entries from PR #23 were read and validated:
  - "Phase 2.5 deferred guards — dependency map" (open; correctly enumerates blockers for the 5 unshipped guards)
  - "Guard pre-parse caching — Phase 2.6 verdict computation" (open; correctly tracks the redundant-parse note in `improvement_observation.py`)
  - "`parse_decimal_string` permissiveness — soft warn now, tighten at Phase 5" (open; correctly tracks the warn→error transition for non-canonical inputs)

**Gaps surfaced:**
- The package-level discipline note explicitly downgrades "fresh list per guard" from runtime enforcement to convention-with-documentation. Per enforcement.md, convention is the weakest mechanism. The PR justifies the downgrade ("rare footgun, code review catches, runtime cost not worth defense-in-depth") and history shows the reversal was deliberate. Not a structural claim, so this is acceptable, but the rationale lives only in the docstring + commit history; cascade-catalog has no entry. Minor.
- `DeprecationWarning` in `parse_decimal_string` is filtered by default in library context (only shown in __main__). Real production runs may not surface the warning. `UserWarning` would be more visible. Worth raising in the review.
- The `practical_delta_guard` is framed in PR description and docstring as "cardinal #10 enforcement," but cardinal #10's PRIMARY enforcement is the rate-based primary endpoint (`failure_improvement_below_threshold`, deferred). The magnitude check is a supplementary defense, not the load-bearing endpoint. Framing question, not a defect.

**Doctrine moments:**
- Applied the **misleading-vs-inconvenient test** to the silent-abstention-on-malformed-DecimalString question. The earlier silent-abstention path would have hidden upstream programmer errors → MISLEADING. The fix (raise InvariantViolationError) is correct per cardinal #1. Confirmed via PR commit `8b74855`.
- Applied the **trust-floor rule**: guards operate above the floor; the floor catches missing-cohort cases via `min_selected_per_required_cohort` against `required_cohorts`. The guards' silent-abstention on missing failure cohort is the right default — they don't substitute for the floor.
- Applied **enforcement-strength hierarchy** to the `Guard` Protocol: type-level prevention (mypy strict on `g: Guard = practical_delta_guard`) is stronger than `@runtime_checkable` would have been. Decision to drop `@runtime_checkable` is correct — the runtime check would have only verified callability, not signature compliance, and would have invited contributors to rely on it for validation.
- Applied **orchestration-not-compute rule**: removal of the `seen_lists` strong-reference shared-list check (commit `064154c`) is consistent with practices.md ("Performance is a follower, not a driver"). The previous defense-in-depth had real cost (allocation per call, complexity), and the footgun is rare. Consistent reversal.

**Notes for the next session:**
- Findings document is at `../findings/PR-23-2026-05-05T12-29-18.md`.
- Subsequent PRs from the deferred-guards list (per cascade entry): the most natural next is the `CohortResult` rate-count extension which unblocks both `baseline_regression_guard` and `failure_improvement_guard` together.
- Worth noting: the `practical_delta_guard` framing-vs-implementation tension (cardinal #10 magnitude vs primary-endpoint rate) should be cleaned up when Phase 2.6 lands `failure_improvement_below_threshold` so the docstrings clearly partition magnitude-layer vs endpoint-layer responsibilities.
